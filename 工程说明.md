# 工程说明

## 项目定位

`open_sow_merge_tool` 是面向游戏配置策划的 Excel 对比工具和多分支 SVN 提交工具。多分支功能的核心是提取“源分支修改前 → 实际提交结果”的修改补丁，再把补丁应用到目标文件；它不会用源文件替换目标文件。最终的 SVN 提交仍由 TortoiseSVN 原生窗口完成。

## 目录结构

```text
src/sow_merge_tool/
  cli.py                 控制台入口
  __main__.py            python -m 入口
  legacy_core.py         现有 Excel 合并核心与 GUI 入口（兼容层）
  branch_submit.py       多分支提交工作台、批次状态和事务恢复
  fast_branch_merge.py   OOXML 源修改分析、目标判定与补丁候选生成
  svn_status_provider.py SVN 状态扫描与 TortoiseSVN 运行库回退
tests/unit/              单元测试及 14 种 SVN 状态策略矩阵
tests/regression/        45 个 Excel/SVN 回归烟测及 Native 验收脚本
tests/integration/       一次性真实 SVN 仓库无界面端到端测试
tools/                   初始化、测试、构建、打包、部署脚本
openspec/specs/          当前有效的功能规格
```

## 开发环境

要求 Windows、Python 3.11 或更高版本、Microsoft Excel（Native 测试需要）、TortoiseSVN 或 `svn.exe`。首次初始化：

```powershell
pwsh -File .\tools\bootstrap.ps1
```

依赖由 `pyproject.toml` 统一管理，不再维护独立的 requirements 文件。

## 测试与构建

```powershell
pwsh -File .\tools\test.ps1 -Profile Fast
pwsh -File .\tools\test.ps1 -Profile Full
pwsh -File .\tools\test.ps1 -Profile Integration  # 无界面真实 SVN 仓库
pwsh -File .\tools\test.ps1 -Profile Native       # 需要 Excel/TortoiseSVN
pwsh -File .\tools\test.ps1 -Profile Visual       # 完整 Tk/Win32 可视回归（显式运行）
pwsh -File .\tools\test.ps1 -Profile Adversarial
pwsh -File .\tools\verify.ps1 -Profile Fast
pwsh -File .\tools\compatibility.ps1 # 使用独立 Python 3.11/3.12 做 compile/import/--help 门禁
python .\tools\ruff_changed.py --base HEAD # 检查当前工作树新增行
pwsh -File .\tools\build.ps1 -Clean
python .\tools\benchmark_ui.py       # 生成忽略目录中的工作台性能数据
```

测试采用分层门禁，避免重复执行相同集合：

- `Fast`：62 个 pytest 单元/状态策略用例，加 7 个关键烟测；用于日常修改和 pre-push，当前约 15 秒。
- `Integration`：14 种 SVN 状态策略矩阵，加一次性 `file://` 真实仓库的状态制造、多分支提交、目标脏文件、部分勾选和服务端成功/客户端报错测试；全程使用 `svn.exe`，不打开界面，当前约 10 秒。
- `Full`：全部 45 个烟测、全部 pytest 和 Integration；用于发布前完整回归。
- `ruff_changed.py`：按明确 Git 基线交叉 Ruff JSON 与提交/工作树新增行，只阻断本次生产改动的新问题；`verify.ps1` 发布门禁自动使用 `HEAD^`。
- `Adversarial`：多分支事务、SVN 冲突/角色语义和 Integration；用于集中攻击事务边界，不再重复全部 Excel GUI 烟测。
- `Fast`、`Integration`、`Adversarial`、`Full`：默认全无界面，使用注入式 Popen/事件循环和 headless SVN；这些 profile 不会启动 sow_merge_tool GUI 进程。
- `Native`：只保留无法无界面证明的关键启动中心、持久配对列表、32 分支/200 文件工作台和真实文件路径布局验收，发布前单独执行。
- `Visual`：关键 Tk/Win32 可视抽检（含 DPI、截图、启动中心、持久列表和逻辑列布局），必须显式执行；历史 UI smoke 可单独运行，默认不调度；运行前后不会强杀业务实例。

`Integration` 首次运行会调用 `tools/setup_svn_test_runtime.ps1`，从 Apache 官方二进制包列表所列的 SlikSVN 获取 1.14.5 x64 便携运行时，校验固定 SHA256 后解包到被 Git 忽略的 `.local/tools`；不安装系统组件、不修改 PATH、不连接业务仓库。

所有测试进程的 `LOCALAPPDATA` 都重定向到 `tmp/test_tmp/appdata`，批次日志、设置和恢复记录不会进入开发者真实的 `%LOCALAPPDATA%\SowMergeTool`；测试脚本退出时恢复原环境变量。

发布编排：

```powershell
pwsh -File .\tools\release.ps1 -SkipDeploy
pwsh -File .\tools\release.ps1
```

发布目录为 `artifacts/release/2026-09-04.update93`，只包含 EXE、中文安装/卸载脚本、使用说明和 SHA256 清单。

## 运行入口

源码入口：

```powershell
python -m sow_merge_tool --help
sow-merge-tool --help
```

冻结入口为 `sow_merge_tool.exe`。右键 `.xlsx/.xlsm`、文件夹或文件夹空白处的“多分支 SVN 提交”统一调用：

```text
sow_merge_tool.exe --branch-submit "<文件或文件夹>"
```

## 设计边界

- 多分支提交是可恢复的分步事务，不宣称跨分支原子提交。
- 未选择目标分支时工作台切换为源分支原生 TortoiseSVN 单分支提交，不走多分支预检查门禁，也不创建批次；选择至少一个目标分支后才启用多分支预检查。
- 修改文件使用 `源修改补丁 = 源提交结果 - 源 SVN 基线`，并以目标文件为候选基础，只写入源分支本次修改的位置；未被源修改触及的目标内容必须保留。
- 目标同一记录/字段仍等于源修改前值时可直接同步；已经等于源新值时跳过；同时不同于两者时必须人工确认采用源修改，或者明确把该目标/文件移出批次。
- 新增文件可以完整复制，因为目标路径原本不存在；修改文件缺少目标派生候选时必须阻断，禁止回退成整文件复制。
- 每个目标工作副本在工具介入前必须干净；冲突、切换、属性修改、锁定或无法确认的状态会阻断批次。
- 源提交窗口关闭后按文件重新核对 SVN 状态；退出码只作提示，不能作为提交事实。
- 修改、新增、明确删除和 missing 分别走安全规则；missing 只进入源分支原生提交窗口，不更新或写入目标，只有 deleted 才传播删除。重命名优先阻断并提示使用 TortoiseSVN Repair Move。
- SVN 原生 14 种状态必须全部出现在表驱动策略测试中；可稳定制造的状态还必须通过一次性真实仓库扫描。官方标记为 unused 的 merged 和只在不完整目录元数据中出现的 incomplete 保留 fail-closed 策略测试，不伪造为业务可提交状态。
- 业务配置目录只做只读扫描和提交演练，不在本工程发布流程中提交业务 Excel。
- `legacy_core.py` 是当前稳定的兼容核心。后续拆分必须先为真实入口建立回归测试，再按功能边界迁移，不能为了“分层”重新制造空壳包。

## 界面与性能约定

- 两个窗口共用 Windows 原生风格的中性界面；场景颜色只作为顶部色条和状态提示，不覆盖配置数据区域。
- 多分支工作台的候选分支按 SVN 最近修改时间倒序，搜索、勾选、预检查和提交均不阻塞 Tk 主线程。
- Excel 首屏优先显示当前工作区，其他 Sheet 和可编辑工作簿在后台继续准备；后台结果必须经过代次校验后才能更新界面。
- 多分支预检查是提交前的必需门禁；第一阶段先检查目标更新范围没有本地改动，再逐分支对修改/明确删除文件和新增文件父目录执行 SVN Update，全部成功后才计算动作矩阵。source-only missing 不产生目标更新路径。目标分支或文件变化会立即使结果失效，正式提交前仍会二次更新和复核。提交说明可以留空预检查并在提交前填写，说明变化不会重复触发文件安全分析。稳定唯一键的尾部新增和已证明的单元格变化走 OOXML 快速补丁路径。
- 恢复中心只接收已进入正式提交、存在目标写入意图或结果未知的批次；恢复前先对账服务端成功结果，禁止把已提交内容反向恢复。TortoiseSVN 遗留的 scheduled Add/Delete 必须逐文件撤销并复核；仅完成目标 SVN 更新、差异分析、预览和人工确认的记录不打断下次启动。多分支工作台底部操作栏独立于可伸缩内容区，在 900×620 及以上窗口保持可见。
- Excel 对比区在每个可见文件上方展示角色、版本、Author 和实际文件路径；两方对比使用严格 50:50 文件卡片，三方展开时才显示三栏，单击路径可复制完整地址。
- Excel 顶部命令栏按窗口宽度自动在标题行和多行命令组之间重排；900×620 仍保证导航、显式方向操作、保存、另存为和“更多”可见，1366/1920 保持紧凑单行或少行布局。多分支目标树和扫描路径提供横向滚动、悬停全文及复制入口。
- 预检查使用“可直接同步、已包含、需人工确认、安全阻断”四类结果。人工确认窗口逐文件展示修改前值、源新值和目标值；确认只记录“采用源修改”的决策，不生成整文件人工合并结果，也不修改目标工作副本。完成项在当前窗口保留标记。
- 预检查矩阵的“查看目标修改点”按需生成目标派生预览，对比的是“目标修改前 ↔ 应用源修改后的目标预览”，不会再把源文件直接当作目标修改后结果，也不会修改目标工作副本。
- Excel 保存产生的选区、视图、列宽、批注关系、条件格式索引和等价换行编码不作为配置修改；合并单元格、数据校验、保护结构、表格结构和无法唯一映射的修改仍会安全阻断。
- 大表候选按工作表成组处理，每张受影响工作表只解析和写回一次；大于 256 KiB 且能证明为直接修改的 `.xlsx` 使用 OOXML 快速路径，无法证明时自动回退到兼容引擎。原生 TortoiseSVN 单分支提交不受预检查门禁影响。
- 使用 `python .\tools\benchmark_ui.py` 生成工作台基准；结果仅写入被 Git 忽略的 `artifacts/performance`，不记录业务配置内容。

## Git 约定

当前直接在 `master` 开发，不创建 PR。提交和推送信息使用中文，例如：

```text
整理：精简工程并统一中文交付
```

日常提交前运行 `tools/verify.ps1 -Profile Fast`。发布统一运行 `tools/release.ps1`：内部只执行一次 `verify -Profile Full`、一次 Native 验收，然后复用该次构建进行打包和部署；不要在 release 前重复手工执行 Full。

## 版本与部署

版本采用 `YYYY-MM-DD.updateNN`，本次为 `2026-09-04.update93`。部署脚本只删除明确列出的旧版工具文件，保留目标目录的 `backups/`、`log/` 和未知用户文件，并在部署前保存受管文件副本。安装状态写入 `%LOCALAPPDATA%\\SowMergeTool\\install-state.json`，卸载时不会覆盖安装后被外部修改的注册表值。启动中心的“Excel 文件比较/合并”右键入口只预填路径；文件夹比较按相对路径逐文件确认，预检查逐文件核对 SVN 服务器 freshness。
