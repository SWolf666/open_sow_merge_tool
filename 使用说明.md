# 使用说明

## 安装与卸载

将发布包解压到任意目录，双击 `安装.bat`。脚本会安装 Excel 差异/合并配置和三个右键入口：

- Excel 文件右键：Excel 文件比较/合并、多分支 SVN 提交
- 文件夹右键：Excel 文件比较/合并、多分支 SVN 提交
- 文件夹空白处右键：Excel 文件比较/合并、多分支 SVN 提交

直接双击 `sow_merge_tool.exe` 会打开启动中心。点击“Excel 文件比较 / 合并”后进入持久的配对列表；打开一行会启动独立比较窗口，列表不会销毁，比较窗口关闭后仍可继续选择其他行。首版同时只允许一个活动比较窗口；关闭配对列表不会强制终止已经打开的比较窗口。右键的“Excel 文件比较/合并”只会把文件或文件夹预填到启动中心，不会自动打开、扫描或写入工作簿。

部署工具或静默安装可执行：

```bat
安装.bat /quiet
安装.bat /context-only /quiet
```

卸载：

```bat
卸载.bat /quiet
卸载.bat /context-only /quiet
```

完整卸载会根据 `%LOCALAPPDATA%\\SowMergeTool\\install-state.json` 恢复安装前的 TortoiseSVN 配置。若发现配置已被用户或其他程序修改，脚本会跳过该项并以中文提示，不会覆盖外部改动。

## Excel 对比与合并

1. 在启动中心选择两侧 Excel 文件，或分别选择两个文件夹后从后台生成的相对路径映射表中明确选择一个文件对。路径不同不会按文件名猜测；缺失/同名异路径可逐行“指定左文件/指定右文件”。同一文件、缺失文件、不可读文件和类型不支持会安全阻断；不同 SVN 仓库、switched、external 和普通导出目录仅作为提示，reparse/external 子树不会被扫描，明确选中的普通 Excel 文件仍可比较。
2. 在工作表和差异导航中查看单元格、行、列及公式差异。每个文件上方会显示实际路径；单击路径可复制完整地址，便于确认“修改前目标文件”和“修改后预览文件”。
3. 使用“仅差异”筛选快速定位变更，双击差异块可跳转。
4. 按需选择源侧、目标侧或手工合并；保存前工具会检查编码、公式缓存和工作簿结构。
5. 另存为新文件并重新打开核对结果，再交给 SVN 提交。

工具不会修改原始文件的 SVN 历史，也不会自动提交工作副本。

## 多分支 SVN 提交

1. 在源分支工作副本中右键 Excel 文件、文件夹或空白处，选择“多分支 SVN 提交”。
2. 工具递归扫描当前范围，列出同一仓库、同一布局的目标分支和文件状态。
3. 搜索并勾选目标分支。选择目标分支表示要把源分支本次修改同步到这些分支，并由 TortoiseSVN 分别提交。
4. 在变更表中勾选要提交的 `.xlsx`；“多分支处理”列会直接显示每个 SVN 状态的目标动作。默认勾选已版本化的修改、新增、删除和 missing，未版本化文件需明确选择；其中 missing 只进入源分支提交窗口，绝不会传播到目标分支。
5. 点击“① 预检查（必需）”。工具会先对每个确实需要传播的目标分支执行 SVN Update：修改/明确删除项只更新对应文件，新增项更新其父目录；仅有 missing 时完全跳过该目标的 Update。所有目标更新成功后，再生成“目标分支 × 文件”的动作矩阵。提交说明此时可以留空，稍后填写或修改不会让预检查失效。选择修改项后可点“查看目标修改点”，看到的是“目标修改前 ↔ 应用源修改后的目标预览”；预览只写入批次目录，不会修改目标工作副本。多分支工作台未完成预检查时不会开放提交按钮。

   预检查对每个选中文件读取源/目标工作副本 BASE revision、服务器端 revision 和当前文件哈希。源分支落后服务器时立即阻断，工具不会自动更新源分支；目标分支只按选中文件更新（新增文件只更新已经验证的父目录），更新后会重新扫描 revision、状态和哈希，无法确认服务器 freshness 时安全停止。
6. 如出现红色“需人工确认”，点击“查看并确认…”。窗口按文件显示工作表、唯一键、字段、修改前值、源分支新值和目标当前值。选择“确认采用源修改”后，只会记录该决策；也可以把该文件从对应目标分支批次中移除。已处理项保留到本次窗口关闭。
7. 填写提交说明（可从 TortoiseSVN 最近消息中选择），然后点击“② 开始提交”。工具先打开源分支 TortoiseSVN 提交窗口并核对实际提交文件，再以目标文件为基础应用源修改补丁，随后逐分支打开 TortoiseSVN 提交窗口。目标分支未被源修改触及的内容会保留。

原生 TortoiseSVN 的 `SVN Commit` 单分支入口保持原有流程，不受多分支工作台的预检查门禁影响。

跨分支提交不是原子事务。进入正式提交后，任何冲突、取消、网络异常或部分文件提交都会停止后续传播，并保留批次记录。下次启动时可查看、继续或恢复未完成批次；工具不会自动回滚已经进入 SVN 仓库的版本。只完成目标路径 SVN Update、差异分析或目标预览的批次不会触发恢复窗口，因为此时尚未写入工具生成的候选内容；正常关闭工作台时会自动结束这类临时批次。

## 状态说明

| 源分支 SVN 状态 | 默认选择 | 目标分支处理 | 安全边界 |
| --- | --- | --- | --- |
| `modified` | 勾选 | 应用源分支单元格修改 | 保留目标其他内容；重叠时人工确认 |
| `added` | 勾选 | 目标路径不存在时新增 | 已存在不同文件则阻断 |
| `unversioned` | 不勾选 | 用户明确勾选后按新增处理 | 不猜测是否应纳入版本控制 |
| `deleted` | 勾选 | 同步明确的 SVN 删除 | 目标有独立内容时人工确认 |
| `missing` | 勾选 | 仅源分支 TortoiseSVN 处理，目标不变 | 不执行目标 Update、删除或写入 |
| 冲突、obstructed、replaced、switched、external、属性修改 | 不可选 | 安全阻断 | 先在 TortoiseSVN 中修复 |

`missing` 只表示版本化文件在磁盘上缺失，并不等于已经执行 SVN Delete。若确实要把删除同步到其他分支，请先在源分支使用 `TortoiseSVN → Delete`，让状态变为 `deleted`；工具不会根据“文件不见了”猜测删除意图。

- 左侧分支状态：绿色“可直接同步”表示补丁可以安全应用；红色“需人工确认”表示目标同一位置存在独立内容；“安全阻断”表示 SVN 状态、文件结构或映射不可靠。状态旁会显示最近修改时间。
- 文件表中的灰色项目：原有改动、未确认状态或不支持的结构，不会自动进入批次。
- 冲突、obstructed、replaced、switched、属性修改、外部项目和锁定文件：必须先在 TortoiseSVN 中处理。
- 新增文件：目标路径必须不存在且父目录已版本化，提交窗口中需要勾选“加入版本控制”；目标已有不同文件时禁止整文件覆盖。
- 删除文件：只有 `deleted` 才会传播删除；目标仍等于删除前基线时可直接删除，目标已有独立内容时必须人工确认。`missing` 始终不修改目标。工具写入前会保存目标副本。
- 重命名：v1 默认阻断，请使用 TortoiseSVN 的“Repair Move”保留历史。
- 状态扫描失败、输出损坏或无法确认 pristine：整批阻断，不以时间戳猜测状态。

## 日志、批次与恢复

批次日志保存在工具数据目录，目标安装目录的 `log/` 和 `backups/` 会在升级时保留。恢复中心只显示已经点击开始提交、写入过目标工作副本、进入过 SVN 提交窗口或结果无法确认的批次。恢复前会先重新对账仓库结果，已经提交成功的文件绝不会被反向恢复；未提交的修改只在候选哈希一致时恢复。若 TortoiseSVN 留下待提交的 Add/Delete，恢复操作会逐文件打开 Revert/Undo Add，并在撤销 SVN 计划状态后再次核对文件和基线。无法确认时保留现场并标记为未知。

## 常见问题

### 右键菜单没有出现

重新双击 `安装.bat`，或执行 `安装.bat /context-only /quiet` 后重启资源管理器。确认 `sow_merge_tool.exe` 与脚本位于同一目录。

### 预检查有什么用，为什么有时仍然不能提交

预检查是多分支提交的必需门禁：它先把本批次涉及的目标文件（新增文件为父目录）更新到 SVN 最新状态，再以只读方式计算源分支本次修改，并判断每个目标分支/文件是“可直接同步、已包含、需人工确认”还是“安全阻断”。若目标更新被取消、失败，或更新范围已有本地改动，预检查会停止并说明具体分支和路径。目标分支或文件在预检查后再次发生变化时，已有结果立即失效；提交说明与文件安全判断无关，因此可以在预检查后填写或修改，但真正提交时不能为空。

人工确认只确认重叠位置采用源分支新值，不会覆盖整个目标文件，也不会立即写入工作副本。点击“开始提交”后才从目标文件生成补丁候选并进入可恢复的分步事务。原生 TortoiseSVN 单分支提交不需要这个门禁。

目标工作副本有本地改动、SVN 状态扫描失败、仓库 UUID/路径布局不一致、源文件在检查后发生变化，或发现冲突/切换/属性修改时会安全阻断。同一记录/字段存在独立修改时要求人工确认；合并单元格、数据校验、表格/保护结构或无法唯一映射的变化会阻断。Excel 保存时产生的选区、视图、列宽、批注关系、条件格式索引和等价换行编码不会被误判成配置修改。

如果不勾选任何目标分支，底部按钮会变为“SVN 单分支提交”。此时工具直接打开源分支的原生 TortoiseSVN 提交窗口，不执行多分支预检查，也不会创建多分支批次；这是单分支提交的快捷入口。

### TortoiseSVN 窗口关闭后没有继续下一个分支

工具检测到取消、部分文件提交或提交结果无法确认。请查看批次详情和 SVN 日志，确认已提交文件后再选择“继续”；不要直接重复复制文件。

### 如何查看历史提交说明

在提交说明区点击“最近提交消息”或“显示日志”。消息按当前仓库 UUID 读取，批次提交会自动追加 `[MultiBranchSync]` 标记和源分支版本。

“最近提交消息”是 TortoiseSVN 的本机输入历史，仅用于填写提交说明；批次对账使用仓库只读结构化日志 `svn log --xml -v`，记录 revision、作者、时间、changed paths、copyfrom 和 mergeinfo。结构化日志读取失败时会标为未知，不会根据消息关键字自动复制、删除或合并文件；没有选择目标分支时仍使用原生 SVN 单分支提交。
